# Nur für Entwicklung und CI — NICHT für den Betrieb nötig.
#
#     pip install -r requirements-dev.txt
#
# Die App selbst braucht davon nichts; `requirements.txt` bleibt die Laufzeit-Wahrheit.
pytest>=7.0,<9

# Liest die erzeugten Reports zurück: ReportLab komprimiert die Textströme, eine Suche
# im Rohbytestrom findet die Herkunftsangaben nicht. Ohne pypdf könnten die Report-Tests
# nur prüfen, DASS ein PDF entsteht — nicht, was darin steht.
pypdf>=4.0,<7

# Linter. Schmale Regelauswahl in ruff.toml — nur echte Fehler plus die Fallen aus
# bugbear, keine Stildebatten. Läuft im schnellen CI-Job in Sekunden.
ruff>=0.16,<1
